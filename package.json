{
  "name": "stock_market_all_in_one",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "prebuild": "node scripts/ensure-placeholder-pngs.js",
    "start": "parcel ./*.html",
    "build": "parcel build ./*.html --dist-dir ./build --public-url ./",
    "build:github-pages": "parcel build ./*.html --dist-dir ./build --public-url /StockMarket-All-In-One/"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "parcel": "^2.7.0"
  }
}